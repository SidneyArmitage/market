use super::connector::industry::fetch_total;
use super::connector::industry::value;

pub fn industry() {
  // get market
  // set market
  // get every industry
  
  // iterate for each
}

pub fn company () {
  // for each company
}